[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Custom markers
markers =
    integration: requires a live test database (deselect with -m "not integration")
    evaluation: requires live LLM API calls (opt-in with --run-eval)

# Async test support
asyncio_mode = auto
